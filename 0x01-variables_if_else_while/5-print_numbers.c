#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int l;

	for (l = 0; l < 10; l++)
	{
		printf("%d", l);
	}
	putchar('\n');
	return (0);
}
