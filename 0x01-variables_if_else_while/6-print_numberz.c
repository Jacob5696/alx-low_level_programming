#include <stdio.h>

/**
 * main - the program prints numbers from 0 to 9.
 *
 * Return: should return 0
 */
int main(void)
{
	int l;

	for (l = 48; l < 58; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}
